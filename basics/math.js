console.log(Math.floor(Math.random() * 100)); // Generates a random number between 0 and 99
console.log(Math.ceil(5.1)); // Rounds up to the nearest integer
console.log(Math.round(5.4)); // Rounds to the nearest integer
console.log(Math.max(10, 20, 30)); // Returns the maximum value
console.log(Math.min(10, 20, 30)); // Returns the minimum value
console.log(Math.sqrt(8)); // Returns the square root of 16
console.log(Math.pow(2, 3)); // Returns 2 raised to the power of 3
console.log(Math.abs(-10)); // Returns the absolute value of -10
console.log(Math.PI); // Returns the value of PI
console.log((Math.random()* 10)+1);

const min = 40
const max = 60
console.log(Math.random() * (max - min + 1))